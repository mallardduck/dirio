# yaml-language-server: $schema=https://goreleaser.com/static/schema.json
# vim: set ts=2 sw=2 tw=0 fo=cnqoj

version: 2

project_name: dirio

before:
  hooks:
    # Sync workspace before anything else so all go.work.sum entries are current.
    - go work sync
    - go mod tidy
    # Install templ CLI pinned to the version in go.mod.
    - go install github.com/a-h/templ/cmd/templ@v0.3.1020
    # Download the Tailwind standalone binary for the current OS/arch.
    - task download-tailwind
    # Regenerate templ outputs and minify CSS from source.
    - task build-ui

builds:
  - id: server
    main: ./cmd/server
    binary: dirio
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X github.com/mallardduck/dirio/common/version.Version={{.Version}}
      - -X github.com/mallardduck/dirio/common/version.Commit={{.Commit}}
      - -X github.com/mallardduck/dirio/common/version.BuildTime={{.Date}}
      - -X github.com/mallardduck/dirio/common/version.BuiltBy=goreleaser
      - -X github.com/mallardduck/dirio/common/version.GitTreeState={{.GitTreeState}}

  - id: client
    main: ./cmd/client
    binary: dio
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X github.com/mallardduck/dirio/common/version.Version={{.Version}}
      - -X github.com/mallardduck/dirio/common/version.Commit={{.Commit}}
      - -X github.com/mallardduck/dirio/common/version.BuildTime={{.Date}}
      - -X github.com/mallardduck/dirio/common/version.BuiltBy=goreleaser
      - -X github.com/mallardduck/dirio/common/version.GitTreeState={{.GitTreeState}}

archives:
  - id: server-archive
    builds:
      - server
    name_template: "dirio_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    formats:
      - tar.gz
    format_overrides:
      - goos: windows
        formats:
          - zip

  - id: client-archive
    builds:
      - client
    name_template: "dio_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    formats:
      - tar.gz
    format_overrides:
      - goos: windows
        formats:
          - zip

release:
  draft: true

checksum:
  name_template: 'checksums.txt'

snapshot:
  version_template: "{{ incpatch .Version }}-next"

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^chore:'